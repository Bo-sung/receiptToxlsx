<Window x:Class="SheetViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SheetViewer"
        mc:Ignorable="d"
        Title="MailMaker_for_LOR" Height="450" Width="800" Background="{DynamicResource {x:Static SystemColors.ActiveCaptionTextBrushKey}}" Foreground="{DynamicResource {x:Static SystemColors.ActiveCaptionTextBrushKey}}">
    
    <Grid>
        <Frame x:Name="frame" Foreground="{x:Null}" Panel.ZIndex="1"  />
        <Button x:Name="SheetModBtn" Content="Sheet" Click="SheetModBtn_Click" Width="100" Height="100" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,10,10,10" Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{x:Null}"/>

    </Grid>
</Window>
    